requests==2.34.2
beautifulsoup4==4.15.0
pytest==8.3.4
psycopg[binary]==3.2.4
pyjwt==2.13.0
passlib[bcrypt]==1.7.4
# pinned below 4.1: passlib 1.7.4's backend detection breaks against bcrypt>=4.1
# (removed bcrypt.__about__), raising "password cannot be longer than 72 bytes"
# on every hash/verify call regardless of password length.
bcrypt==4.0.1
