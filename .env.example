DATABASE_URL=postgresql://rag:ragdev@localhost:5433/rag
OLLAMA_URL=http://localhost:11434
EMBED_MODEL=nomic-embed-text
# Generation provider for the deployed demo: ollama (local dev) or groq (hosted)
LLM_PROVIDER=ollama
# Get a free key at console.groq.com - required only when LLM_PROVIDER=groq
GROQ_API_KEY=gsk_your_key_here
# Any long random string. Generate one: python -c "import secrets;print(secrets.token_urlsafe(48))"
JWT_SECRET=change-me-to-a-long-random-string
